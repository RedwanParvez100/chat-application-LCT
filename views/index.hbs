<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Application - Home</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #e5e5e5;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            text-align: center;
            padding: 2rem;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: #000;
        }

        .welcome-text {
            font-style: italic;
            margin-bottom: 2rem;
            font-size: 1.2rem;
        }

        .auth-links {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            align-items: center;
        }

      .link {
    background: none;
    border: none;
    color: #2d1810;
    text-decoration: none;
    font-size: larger;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 5px 10px;
    border-radius: 15px;
}

.link:hover {
    background: rgba(45, 24, 16, 0.1);
    text-decoration: underline;
}

        .signup-text {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Chat Application</h1>
        <p class="welcome-text">Welcome Everyone!</p>
        <a href="/login" class="btn link">Login</a>
        <p class="signup-text"> Don't have an account? <a href="/register" class="btn link">Sign Up</a></p>
        
    </div>
</body>
</html>